Feature: Verify drag and drop functionality
    
    @mouseDragdrp
    Scenario: Verify drag and drop
        Given I launch the broswer
        #Then I handle dragDrop via mouse hover
        #Then I handle dragDrop via playwright
        Then I handle dragDrop via mouse hover in practice website
        #Then I handle dragDrop via playwright in practice website